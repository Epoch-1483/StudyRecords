[[反射]]
![[Pasted image 20251102204204.png]]

自定义返回单个构造方法对象：
	在括号里面写上构造方法括号中的类型在加上.class（字节码文件）
	多个参数也用逗号隔开

访问权限修饰符：
```
int modifies = con4.getModifiers();
sout(modifies)
```

访问每个 参数：
```
Parameter[] parameters = con4.getParameters();
// 总增强for循环打印参数
```
Declared返回所有构造方法但只能看到不能直接使用
暴力反射：

![[Pasted image 20251102210240.png]]