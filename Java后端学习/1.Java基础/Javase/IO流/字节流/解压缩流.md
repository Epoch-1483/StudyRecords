压缩包里面的每一个文件都是  ==ZipEntry==

解压的本质：
			把每一个ZipEntry 按照层级拷贝到本地另外一个文件夹中

zip.getNextEntry()

```
// 1.创建一个要解压的压缩包  
    File src = new File("D:\\aaa.zip");  
    // 2.创建一个解压的的目的地  
    File dest = new File("D:\\");  
  
}  
// 定义一个方法 用来解压  
public static void unzip(File src, File dest) throws IOException {  
    //解压的本质：是把每一个ZipEntry 按照层级拷贝到本地另外一个文件夹中  
  
    // 创建解压缩流==ZipEntry==把本地文件读取到程序中来ZipInputStream  
    ZipInputStream zip = new ZipInputStream(new FileInputStream(src));  
  
    ZipEntry entry;  
    while ((entry = zip.getNextEntry()) != null){  
        System.out.println(entry);  
        // 对文件和文件夹判断  
        if (entry.isDirectory()){  
            // 是文件夹需要再目的地创建一个同样的文件夹  
            // entry 是ZipEntry类型 需要转换为字符串  
            File file = new File(dest,entry.toString());  
            // 创建多级文件夹  
            file.mkdirs();  
        }else {  
            // 读取压缩包的中的文件,放到目的地dest 文件夹中按照层级目录存放  
            FileOutputStream fos = new FileOutputStream(new File(dest,entry.toString()));  
            int b;  
            while ((b = zip.read()) != -1){  
                fos.write(b);  
            }  
            fos.close();  
            // 表示文件压缩包中的一个文件处理完毕了  
            zip.closeEntry();  
        }  
    }  
    zip.close();
```



---

## 🔗 关联笔记
- [[IO流]]
