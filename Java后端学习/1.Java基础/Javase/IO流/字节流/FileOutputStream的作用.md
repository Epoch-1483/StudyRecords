`OutputStream` 是所有**字节输出流**的抽象基类，用于将字节数据写入**目标**（如文件、网络、内存等）。
操作本地文件的输出流，可以把==程序中的数据==写到==本地文件==中

三种方式：
![[Pasted image 20251026184625.png]]
第三种方式细节：
				参数1：是数组
				参数2：起始索引
				参数3：个数 


写数据的两个小问题：
			  1.换行写：
				再次写一个换行符
					Windows ： \r\n
					 Linux : \n
					 Mac : \r
			   细节：
				在Windows操作系统中，Java对回车换行进行了优化。
				只写其中一个就行了
			  2. 换行写：
				  在创建对象的第二个参数写上true，默认是false
			  
			
最后的总结：
![[Pasted image 20251026190916.png]]
			 ==释放资源==：关闭通道